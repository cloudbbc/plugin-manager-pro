# Plugin Manager Pro 更新日志 v5.1.6

## 本次更新内容

### 1. 核心功能改进：PMP配置清单作为唯一真理源

#### 改变自启状态切换逻辑
- **之前**：切换自启状态时直接操作Blender的preferences.addons，立即生效
- **现在**：切换自启状态仅更新PMP配置文件，不直接操作系统状态
- **优势**：
  - PMP维护自己的持久化配置副本
  - Blender启动时根据PMP配置重新初始化系统插件启用状态
  - 使用Blender原生启用/停用机制，加强插件兼容性
  - 会话隔离：当前会话中临时加载的插件不会污染下次启动

#### 启动时接管机制
在`__init__.py`的`_enforce_startup()`函数中：
- 首次使用：将当前所有已启用插件的自启状态同步到PMP配置
- 非首次使用：PMP完全接管系统，根据PMP配置重新初始化所有插件状态
  - PMP标记为自启 → 确保插件启用并设为自启
  - PMP标记为非自启 → 确保插件停用并从preferences.addons移除

### 2. 分类面板采用Blender原生方式

#### 插件加载/停用
- 在分类面板（Header弹出菜单）中，使用Blender原生的enable/disable方式
- 插件被打勾或取消打勾改变状态
- **重要**：这种临时的加载/停用操作不会被保存到PMP配置中
- 重启后，PMP会根据自身的配置清单重新初始化系统状态

#### 简化操作，加强兼容性
- 用户无需关心复杂的自启管理
- 按需加载的插件在当前会话有效
- 重启后自动恢复到PMP配置的状态

### 3. 取消隐藏已加载的自启插件

#### 修改前的问题
- 在Header的分类图标弹出菜单中，设置为自启的插件会被隐藏
- 用户无法看到和管理这些插件

#### 修改后的行为
- 在`header.py`的`_get_addons_for_category()`函数中移除了跳过自启插件的逻辑
- 所有属于该分类的插件都会显示，无论其自启状态如何
- 用户可以清楚地看到所有插件，并进行加载/停用操作

### 4. 新增HTML帮助文档

创建了`help.html`文件，包含：
- 完整的功能介绍和使用说明
- 精美的响应式设计
- 包含收款码图片（Image/VX.jpg）
- 涵盖以下内容：
  - 为什么需要Plugin Manager Pro
  - 核心功能详解
  - 使用场景示例
  - 安装要求
  - 打赏支持信息

## 技术实现细节

### 修改的文件

1. **`__init__.py`**
   - 重写`_enforce_startup()`函数
   - 实现PMP配置接管系统插件管理的逻辑

2. **`operators.py`**
   - 修改`PMP_OT_toggle_startup`操作符
   - 现在只更新PMP配置，不直接操作系统状态
   - 添加提示"重启后生效"

3. **`config_panel.py`**
   - 修改`refresh_addon_list()`函数
   - 自启状态以PMP配置为准，而非Blender的preferences
   - 确保UI显示与PMP配置一致

4. **`header.py`**
   - 修改`_get_addons_for_category()`函数
   - 移除跳过自启插件的逻辑
   - 所有插件都显示在分类列表中

5. **`help.html`** (新建)
   - 完整的HTML帮助文档
   - 包含收款码图片和完整功能说明

## 使用建议

### 最佳实践

1. **设置自启插件**
   - 只有真正"必要"的插件（每次都要用）才设为自启
   - 通过PMP主面板的自启开关设置
   - 重启Blender后生效

2. **按需加载插件**
   - 其他插件保持非自启状态
   - 需要时通过Header分类图标快速加载
   - 用完即停，释放资源

3. **会话隔离**
   - 当前会话中临时加载的插件不会影响下次启动
   - 即使忘记停用，重启后也会自动清理
   - PMP配置清单是唯一真理源

### 工作流程示例

```
1. 首次安装PMP
   ↓
2. PMP自动同步当前已启用插件的自启状态
   ↓
3. 在PMP主面板中调整自启设置
   - 常用插件：开启自启
   - 偶尔使用的插件：关闭自启
   ↓
4. 重启Blender
   ↓
5. PMP根据配置重新初始化系统状态
   - 自启插件自动加载
   - 非自启插件保持停用
   ↓
6. 日常使用中
   - 通过Header分类图标按需加载插件
   - 用完即停
   ↓
7. 下次重启
   - 回到步骤5，状态完全由PMP配置决定
```

## 兼容性说明

- ✅ 完全兼容Blender原生插件管理系统
- ✅ 不影响其他插件的正常运行
- ✅ 使用Blender原生的enable/disable机制
- ✅ 配置文件存储在Blender用户配置目录下
- ✅ 支持Blender 4.5+扩展系统

## 已知限制

- 自启状态切换需要重启Blender才能完全生效
- 当前会话中的临时加载/停用不会被保存
- 这是设计特性，用于保证会话隔离和系统稳定性

## 未来计划

- [ ] 添加一键恢复初始状态功能
- [ ] 支持插件配置文件的导入/导出
- [ ] 添加插件使用统计功能
- [ ] 优化插件加载性能

---

**更新日期**: 2026-06-10  
**版本**: v5.1.6  
**作者**: terryye
